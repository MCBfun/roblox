game:GetService("StarterGui"):SetCore("SendNotification", {
    Title = "公告",
    Text = "给个三连吧！",
    Icon = "rbxassetid://75412502671766",
    Duration = 2,
    Position = UDim2.new(0, 20, 1, -50)
})
game:GetService("StarterGui"):SetCore("SendNotification", {
    Title = "公告",
    Text = "渔炒鱼",
    Icon = "rbxassetid://98446998103932",
    Duration = 3,
    Position = UDim2.new(0, 20, 1, -50)
})

-- 检查本地玩家
local Players = game:GetService("Players")
local localPlayer
local success, err = pcall(function()
    localPlayer = Players.LocalPlayer
end)

if not success then
    game:GetService("StarterGui"):SetCore("SendNotification", {
        Title = "错误",
        Text = "无法获取本地玩家",
        Duration = 5
    })
    return
end

-- 等待PlayerGui
local PlayerGui
local success2, err2 = pcall(function()
    PlayerGui = localPlayer:WaitForChild("PlayerGui", 10)
end)

if not success2 then
    game:GetService("StarterGui"):SetCore("SendNotification", {
        Title = "错误",
        Text = "无法加载玩家界面",
        Duration = 5
    })
    return
end

local UserInputService = game:GetService("UserInputService")

-- 创建UI
local ui = Instance.new("ScreenGui")
ui.Name = "PlaybreakToolbox"
ui.Parent = PlayerGui
ui.IgnoreGuiInset = false
ui.ResetOnSpawn = false

-- 浮动按钮（用于重新打开UI）- 更小尺寸
local floatButton = Instance.new("ImageButton")
floatButton.Size = UDim2.new(0, 45, 0, 45)  -- 进一步缩小到45x45
floatButton.Position = UDim2.new(0, 10, 0.9, 0)  -- 贴近右下角
floatButton.BackgroundColor3 = Color3.fromRGB(30, 136, 229)
floatButton.Image = "rbxassetid://3926305904"
floatButton.ImageRectOffset = Vector2.new(884, 4)
floatButton.ImageRectSize = Vector2.new(36, 36)
floatButton.Visible = true
floatButton.Parent = ui

-- 浮动按钮圆角
local floatCorner = Instance.new("UICorner")
floatCorner.CornerRadius = UDim.new(1, 0)
floatCorner.Parent = floatButton

-- 主面板 - 再次缩小尺寸
local panel = Instance.new("Frame")
panel.Size = UDim2.new(0, 280, 0, 320)  -- 从300x360缩小到280x320
panel.Position = UDim2.new(0.5, -140, 0.5, -160)
panel.BackgroundColor3 = Color3.fromRGB(25, 25, 30)
panel.BorderSizePixel = 0
panel.Parent = ui

-- 添加圆角效果
local corner = Instance.new("UICorner")
corner.CornerRadius = UDim.new(0, 8)  -- 从10缩小到8
corner.Parent = panel

-- 添加简洁阴影效果
local shadow = Instance.new("UIStroke")
shadow.Color = Color3.fromRGB(0, 150, 255)
shadow.Thickness = 1  -- 从2缩小到1
shadow.Parent = panel

-- 标题栏 - 再次缩小
local titleBar = Instance.new("Frame")
titleBar.Size = UDim2.new(1, 0, 0, 40)  -- 从45缩小到40
titleBar.BackgroundColor3 = Color3.fromRGB(30, 30, 40)
titleBar.BorderSizePixel = 0
titleBar.Parent = panel

local titleCorner = Instance.new("UICorner")
titleCorner.CornerRadius = UDim.new(0, 8)  -- 从10缩小到8
titleCorner.Parent = titleBar

-- 标题栏渐变效果（简化）
local titleGradient = Instance.new("UIGradient")
titleGradient.Color = ColorSequence.new({
    ColorSequenceKeypoint.new(0, Color3.fromRGB(30, 136, 229)),
    ColorSequenceKeypoint.new(1, Color3.fromRGB(100, 77, 245))
})
titleGradient.Rotation = 90
titleGradient.Parent = titleBar

-- 标题文字 - 再次缩小
local title = Instance.new("TextLabel")
title.Size = UDim2.new(0.65, 0, 1, 0)  -- 从0.7缩小到0.65
title.Position = UDim2.new(0.05, 0, 0, 0)
title.BackgroundTransparency = 1
title.Text = "⚡ PLAYBREAK"
title.TextColor3 = Color3.fromRGB(255, 255, 255)
title.TextSize = 14  -- 从16缩小到14
title.Font = Enum.Font.GothamBold
title.TextXAlignment = Enum.TextXAlignment.Left
title.Parent = titleBar

-- 版本信息 - 再次缩小
local versionLabel = Instance.new("TextLabel")
versionLabel.Size = UDim2.new(0.3, 0, 1, 0)  -- 从0.25调整到0.3
versionLabel.Position = UDim2.new(0.7, 0, 0, 0)  -- 从0.75调整到0.7
versionLabel.BackgroundTransparency = 1
versionLabel.Text = "v2.0"
versionLabel.TextColor3 = Color3.fromRGB(200, 200, 200)
versionLabel.TextSize = 10  -- 从11缩小到10
versionLabel.Font = Enum.Font.Gotham
versionLabel.TextXAlignment = Enum.TextXAlignment.Right
versionLabel.Parent = titleBar

-- 关闭按钮 - 再次缩小
local closeBtn = Instance.new("TextButton")  -- 改用TextButton简化
closeBtn.Size = UDim2.new(0, 24, 0, 24)  -- 从28缩小到24
closeBtn.Position = UDim2.new(0.93, 0, 0.2, 0)
closeBtn.BackgroundColor3 = Color3.fromRGB(255, 60, 60)
closeBtn.Text = "×"
closeBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
closeBtn.TextSize = 16  -- 从20缩小到16
closeBtn.Font = Enum.Font.GothamBold
closeBtn.Parent = titleBar

local closeCorner = Instance.new("UICorner")
closeCorner.CornerRadius = UDim.new(1, 0)
closeCorner.Parent = closeBtn

-- 状态显示区域 - 再次缩小
local statusContainer = Instance.new("Frame")
statusContainer.Size = UDim2.new(1, -16, 0, 60)  -- 从70缩小到60，边距从20缩小到16
statusContainer.Position = UDim2.new(0, 8, 0, 48)  -- 从55调整到48
statusContainer.BackgroundColor3 = Color3.fromRGB(40, 40, 45)
statusContainer.BorderSizePixel = 0
statusContainer.Parent = panel

local statusCorner = Instance.new("UICorner")
statusCorner.CornerRadius = UDim.new(0, 6)  -- 从8缩小到6
statusCorner.Parent = statusContainer

-- 状态标题 - 再次缩小
local statusTitle = Instance.new("TextLabel")
statusTitle.Size = UDim2.new(1, 0, 0, 20)  -- 从22缩小到20
statusTitle.Position = UDim2.new(0, 0, 0, 0)
statusTitle.BackgroundTransparency = 1
statusTitle.Text = "📊 状态"
statusTitle.TextColor3 = Color3.fromRGB(200, 200, 255)
statusTitle.TextSize = 12  -- 从13缩小到12
statusTitle.Font = Enum.Font.GothamBold
statusTitle.TextXAlignment = Enum.TextXAlignment.Left
statusTitle.Parent = statusContainer

-- 状态分隔线
local statusDivider = Instance.new("Frame")
statusDivider.Size = UDim2.new(1, -12, 0, 1)  -- 边距从20缩小到12
statusDivider.Position = UDim2.new(0, 6, 0, 20)  -- 调整位置
statusDivider.BackgroundColor3 = Color3.fromRGB(80, 80, 100)
statusDivider.BorderSizePixel = 0
statusDivider.Parent = statusContainer

-- 状态文字 - 再次缩小
local statusText = Instance.new("TextLabel")
statusText.Size = UDim2.new(1, -12, 0, 36)  -- 从44缩小到36
statusText.Position = UDim2.new(0, 6, 0, 22)  -- 调整位置
statusText.BackgroundTransparency = 1
statusText.Text = "🟢 就绪"
statusText.TextColor3 = Color3.fromRGB(220, 220, 220)
statusText.TextSize = 11  -- 从12缩小到11
statusText.Font = Enum.Font.Gotham
statusText.TextWrapped = true
statusText.TextXAlignment = Enum.TextXAlignment.Left
statusText.TextYAlignment = Enum.TextYAlignment.Top
statusText.Parent = statusContainer

-- 按钮容器 - 再次缩小
local buttonContainer = Instance.new("Frame")
buttonContainer.Size = UDim2.new(1, -16, 0, 180)  -- 从200缩小到180
buttonContainer.Position = UDim2.new(0, 8, 0, 116)  -- 从135调整到116
buttonContainer.BackgroundTransparency = 1
buttonContainer.Parent = panel

-- 按钮样式函数（最小化版本）
local function createStyledButton(name, icon, color, position, size)
    local button = Instance.new("Frame")
    button.Size = size
    button.Position = position
    button.BackgroundColor3 = Color3.fromRGB(40, 40, 45)
    button.BorderSizePixel = 0
    button.Parent = buttonContainer
    
    local buttonCorner = Instance.new("UICorner")
    buttonCorner.CornerRadius = UDim.new(0, 5)  -- 从6缩小到5
    buttonCorner.Parent = button
    
    -- 按钮边框（更细）
    local buttonBorder = Instance.new("UIStroke")
    buttonBorder.Color = color
    buttonBorder.Thickness = 1  -- 从1.5缩小到1
    buttonBorder.Parent = button
    
    -- 按钮内容
    local buttonContent = Instance.new("TextButton")
    buttonContent.Size = UDim2.new(1, 0, 1, 0)
    buttonContent.BackgroundTransparency = 1
    buttonContent.Text = ""
    buttonContent.AutoButtonColor = false
    buttonContent.Parent = button
    
    -- 图标 - 最小化
    local buttonIcon = Instance.new("TextLabel")
    buttonIcon.Size = UDim2.new(0, 22, 1, 0)  -- 从26缩小到22
    buttonIcon.Position = UDim2.new(0, 6, 0, 0)  -- 从8缩小到6
    buttonIcon.BackgroundTransparency = 1
    buttonIcon.Text = icon
    buttonIcon.TextColor3 = color
    buttonIcon.TextSize = 18  -- 从20缩小到18
    buttonIcon.Font = Enum.Font.GothamBold
    buttonIcon.TextXAlignment = Enum.TextXAlignment.Left
    buttonIcon.Parent = buttonContent
    
    -- 按钮文字 - 最小化
    local buttonLabel = Instance.new("TextLabel")
    buttonLabel.Size = UDim2.new(1, -34, 1, 0)  -- 从42调整到34
    buttonLabel.Position = UDim2.new(0, 34, 0, 0)  -- 从42调整到34
    buttonLabel.BackgroundTransparency = 1
    buttonLabel.Text = name
    buttonLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
    buttonLabel.TextSize = 13  -- 从14缩小到13
    buttonLabel.Font = Enum.Font.GothamBold
    buttonLabel.TextXAlignment = Enum.TextXAlignment.Left
    buttonLabel.Parent = buttonContent
    
    -- 按钮悬停效果（简化版）
    local buttonHover = Instance.new("Frame")
    buttonHover.Size = UDim2.new(1, 0, 1, 0)
    buttonHover.BackgroundColor3 = color
    buttonHover.BackgroundTransparency = 0.95  -- 更透明
    buttonHover.BorderSizePixel = 0
    buttonHover.Visible = false
    buttonHover.Parent = buttonContent
    
    local hoverCorner = Instance.new("UICorner")
    hoverCorner.CornerRadius = UDim.new(0, 5)  -- 从6缩小到5
    hoverCorner.Parent = buttonHover
    
    buttonContent.MouseEnter:Connect(function()
        buttonHover.Visible = true
        buttonBorder.Thickness = 1.5  -- 从2缩小到1.5
    end)
    
    buttonContent.MouseLeave:Connect(function()
        buttonHover.Visible = false
        buttonBorder.Thickness = 1
    end)
    
    return buttonContent
end

-- 创建按钮 - 再次缩小
local cleanBtn = createStyledButton("开始执行", "🔧", Color3.fromRGB(76, 175, 80),
    UDim2.new(0, 0, 0, 0), UDim2.new(1, 0, 0, 45))  -- 从50缩小到45

local instantBtn = createStyledButton("秒互动", "⚡", Color3.fromRGB(0, 150, 255),
    UDim2.new(0, 0, 0, 50), UDim2.new(1, 0, 0, 45))  -- 间距从55缩小到50

local autoInteractBtn = createStyledButton("自动互动", "🤖", Color3.fromRGB(156, 39, 176),
    UDim2.new(0, 0, 0, 100), UDim2.new(1, 0, 0, 45))  -- 间距从110缩小到100

local closeUIBtn = createStyledButton("关闭界面", "❌", Color3.fromRGB(200, 50, 50),
    UDim2.new(0, 0, 0, 150), UDim2.new(1, 0, 0, 45))  -- 间距从165缩小到150

-- 底部信息栏 - 再次缩小
local footer = Instance.new("Frame")
footer.Size = UDim2.new(1, -16, 0, 22)  -- 从25缩小到22
footer.Position = UDim2.new(0, 8, 1, -30)  -- 调整位置
footer.BackgroundColor3 = Color3.fromRGB(40, 40, 45)
footer.BorderSizePixel = 0
footer.Parent = panel

local footerCorner = Instance.new("UICorner")
footerCorner.CornerRadius = UDim.new(0, 5)  -- 从6缩小到5
footerCorner.Parent = footer

-- 底部文字 - 再次缩小
local footerText = Instance.new("TextLabel")
footerText.Size = UDim2.new(1, 0, 1, 0)
footerText.BackgroundTransparency = 1
footerText.Text = "✨ 渔炒鱼 | F1-F4"
footerText.TextColor3 = Color3.fromRGB(180, 180, 200)
footerText.TextSize = 10  -- 从11缩小到10
footerText.Font = Enum.Font.Gotham
footerText.Parent = footer

-- 浮动按钮悬停效果（简化版）
floatButton.MouseEnter:Connect(function()
    floatButton.BackgroundColor3 = Color3.fromRGB(40, 160, 240)
end)

floatButton.MouseLeave:Connect(function()
    floatButton.BackgroundColor3 = Color3.fromRGB(30, 136, 229)
end)

-- 拖动功能
local dragging = false
local dragStart, startPos

titleBar.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 then
        dragging = true
        dragStart = input.Position
        startPos = panel.Position
        
        input.Changed:Connect(function()
            if input.UserInputState == Enum.UserInputState.End then
                dragging = false
            end
        end)
    end
end)

UserInputService.InputChanged:Connect(function(input)
    if dragging and input.UserInputType == Enum.UserInputType.MouseMovement then
        local delta = input.Position - dragStart
        panel.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y)
    end
end)

-- 浮动按钮点击展开面板
floatButton.MouseButton1Click:Connect(function()
    panel.Visible = true
    floatButton.Visible = false
end)

-- 关闭按钮功能（关闭面板，显示浮动按钮）
closeBtn.MouseButton1Click:Connect(function()
    panel.Visible = false
    floatButton.Visible = true
end)

-- ===========================================
-- 开始执行功能（清理脚本）
-- ===========================================
cleanBtn.MouseButton1Click:Connect(function()
    local character = localPlayer.Character
    if not character then
        statusText.Text = "🔴 错误：未找到角色"
        statusText.TextColor3 = Color3.fromRGB(255, 100, 100)
        return
    end
    
    statusText.Text = "🔄 扫描中..."
    statusText.TextColor3 = Color3.fromRGB(255, 200, 100)
    
    -- 在新线程中执行
    spawn(function()
        local success, result = pcall(function()
            -- 清理脚本
            local removedCount = 0
            local allDescendants = character:GetDescendants()
            
            -- 先统计总数
            local totalScripts = 0
            for _, descendant in ipairs(allDescendants) do
                if descendant:IsA("Script") or descendant:IsA("LocalScript") then
                    totalScripts = totalScripts + 1
                end
            end
            
            statusText.Text = string.format("📊 %d 个脚本\n清理中...", totalScripts)
            
            -- 开始清理
            for i, descendant in ipairs(allDescendants) do
                if descendant:IsA("Script") or descendant:IsA("LocalScript") then
                    descendant:Destroy()
                    removedCount = removedCount + 1
                    
                    -- 更新进度
                    if removedCount % 5 == 0 then
                        statusText.Text = string.format("🔄 进度: %d/%d", removedCount, totalScripts)
                    end
                end
                
                -- 避免卡顿
                if i % 20 == 0 then
                    task.wait(0.01)
                end
            end
            
            return removedCount
        end)
        
        if success then
            if type(result) == "number" then
                statusText.Text = string.format("✅ 完成！\n移除 %d 个", result)
                statusText.TextColor3 = Color3.fromRGB(100, 255, 100)
                
                -- 发送通知
                game:GetService("StarterGui"):SetCore("SendNotification", {
                    Title = "清理完成",
                    Text = string.format("移除了 %d 个脚本", result),
                    Duration = 3,
                    Icon = "rbxassetid://139532409537387"
                })
            else
                statusText.Text = "❌ 错误"
                statusText.TextColor3 = Color3.fromRGB(255, 100, 100)
            end
        else
            statusText.Text = "❌ 操作失败"
            statusText.TextColor3 = Color3.fromRGB(255, 100, 100)
        end
        
        task.wait(3)
        statusText.Text = "🟢 就绪"
        statusText.TextColor3 = Color3.fromRGB(220, 220, 220)
    end)
end)

-- ===========================================
-- 自动互动功能
-- ===========================================
local autoInteractEnabled = false
local autoInteractConnection = nil
local autoInteractRange = 50

local function scanAndInteract()
    if not autoInteractEnabled then return end
    
    local character = localPlayer.Character
    if not character then return end
    
    local rootPart = character:FindFirstChild("HumanoidRootPart")
    if not rootPart then return end
    
    local interacted = 0
    
    -- 扫描范围内的所有互动物体
    for _, obj in pairs(workspace:GetDescendants()) do
        if not autoInteractEnabled then break end
        
        local canInteract = false
        
        -- 检查是否是互动物体
        if obj:IsA("ClickDetector") then
            canInteract = true
        elseif obj:IsA("ProximityPrompt") then
            canInteract = true
        elseif obj.Name:lower():find("button") or 
               obj.Name:lower():find("click") or
               obj.Name:lower():find("interact") or
               obj.Name:lower():find("collect") or
               obj.Name:lower():find("pickup") then
            canInteract = true
        end
        
        if canInteract then
            local distance = 0
            if obj:IsA("BasePart") then
                distance = (rootPart.Position - obj.Position).Magnitude
            elseif obj.Parent and obj.Parent:IsA("BasePart") then
                distance = (rootPart.Position - obj.Parent.Position).Magnitude
            end
            
            if distance <= autoInteractRange then
                pcall(function()
                    if obj:IsA("ClickDetector") then
                        fireclickdetector(obj)
                    elseif obj:IsA("ProximityPrompt") then
                        obj:InputHoldBegin()
                        task.wait(0.01)
                        obj:InputHoldEnd()
                    else
                        for _, child in pairs(obj:GetDescendants()) do
                            if child:IsA("ClickDetector") then
                                fireclickdetector(child)
                                break
                            end
                        end
                    end
                end)
                
                interacted = interacted + 1
                
                if interacted % 10 == 0 then
                    task.wait(0.05)
                end
            end
        end
    end
    
    if interacted > 0 and autoInteractEnabled then
        statusText.Text = string.format("🤖 互动中\n%d 个物体", interacted)
        statusText.TextColor3 = Color3.fromRGB(200, 100, 255)
    end
end

local function toggleAutoInteract()
    if autoInteractEnabled then
        -- 关闭自动互动
        autoInteractEnabled = false
        if autoInteractConnection then
            autoInteractConnection:Disconnect()
            autoInteractConnection = nil
        end
        
        statusText.Text = "✅ 已停止"
        statusText.TextColor3 = Color3.fromRGB(200, 200, 200)
        
        game:GetService("StarterGui"):SetCore("SendNotification", {
            Title = "自动互动",
            Text = "已停止",
            Duration = 2,
            Icon = "rbxassetid://7734068321"
        })
    else
        -- 启用自动互动
        autoInteractEnabled = true
        
        statusText.Text = "🔄 启动中..."
        statusText.TextColor3 = Color3.fromRGB(200, 100, 255)
        
        -- 启动自动互动循环
        autoInteractConnection = game:GetService("RunService").Heartbeat:Connect(function()
            if autoInteractEnabled then
                scanAndInteract()
            end
        end)
        
        -- 成功提示
        task.wait(0.5)
        if autoInteractEnabled then
            statusText.Text = "✅ 已启用\n自动互动"
            statusText.TextColor3 = Color3.fromRGB(100, 255, 100)
            
            game:GetService("StarterGui"):SetCore("SendNotification", {
                Title = "自动互动",
                Text = "已启用！",
                Duration = 3,
                Icon = "rbxassetid://7734068321"
            })
        end
    end
end

autoInteractBtn.MouseButton1Click:Connect(toggleAutoInteract)

-- ===========================================
-- 秒互动功能
-- ===========================================
local instantInteractionEnabled = false

local function enableInstantInteraction()
    if instantInteractionEnabled then
        statusText.Text = "⚠️ 已启用"
        return
    end
    
    statusText.Text = "🔄 正在启用中..."
    statusText.TextColor3 = Color3.fromRGB(100, 200, 255)
    
    spawn(function()
        local success, err = pcall(function()
            -- 超轻量秒互动脚本 - 一行代码
            getgenv().fireclickdetector = function(clickDetector, ...)
                if clickDetector and clickDetector:IsA("ClickDetector") then
                    (getrenv().fireclickdetector or fireclickdetector)(clickDetector, ...)
                end
            end
            
-- 移除所有互动延迟
while true do
for _, v in pairs(game:GetDescendants()) do
                if v:IsA("ProximityPrompt") then
                    v.HoldDuration = 0
                end
            end
wait(5)
end
            -- 点击即完成
            local mouse = game:GetService("Players").LocalPlayer:GetMouse()
            mouse.Button1Down:Connect(function()
                local target = mouse.Target
                if target then
                    local clicker = target:FindFirstChildOfClass("ClickDetector")
                    if clicker then
                        fireclickdetector(clicker)
                    end
                    
                    local prompt = target:FindFirstChildOfClass("ProximityPrompt")
                    if prompt then
                        pcall(function()
                            prompt.HoldDuration = 0
                            prompt:InputHoldBegin()
                            prompt:InputHoldEnd()
                        end)
                    end
                end
            end)
            
            instantInteractionEnabled = true
            
            statusText.Text = "✅ 已启用\n点击即完成"
            statusText.TextColor3 = Color3.fromRGB(100, 255, 100)
            
            game:GetService("StarterGui"):SetCore("SendNotification", {
                Title = "秒互动",
                Text = "已启用！",
                Duration = 3,
                Icon = "rbxassetid://7734068321"
            })
        end)
        
        if not success then
            statusText.Text = "❌ 失败"
            statusText.TextColor3 = Color3.fromRGB(255, 100, 100)
        end
        
        wait(3)
        if instantInteractionEnabled then
            statusText.Text = "✅ 已启用"
            statusText.TextColor3 = Color3.fromRGB(100, 255, 100)
        else
            statusText.Text = "🟢 就绪"
            statusText.TextColor3 = Color3.fromRGB(220, 220, 220)
        end
    end)
end

instantBtn.MouseButton1Click:Connect(enableInstantInteraction)

-- ===========================================
-- 关闭UI按钮功能
-- ===========================================
closeUIBtn.MouseButton1Click:Connect(function()
    -- 先停止自动互动
    if autoInteractEnabled then
        toggleAutoInteract()
        wait(0.2)
    end
    
    statusText.Text = "🔄 关闭中..."
    statusText.TextColor3 = Color3.fromRGB(255, 200, 100)
    
    wait(0.5)
    
    -- 完全销毁UI
    ui:Destroy()
    
    game:GetService("StarterGui"):SetCore("SendNotification", {
        Title = "提示",
        Text = "已关闭",
        Duration = 2
    })
end)

-- ===========================================
-- 键盘快捷键
-- ===========================================
UserInputService.InputBegan:Connect(function(input, processed)
    if processed then return end
    
    if input.KeyCode == Enum.KeyCode.F1 then
        cleanBtn:MouseButton1Click()
    elseif input.KeyCode == Enum.KeyCode.F2 then
        if not instantInteractionEnabled then
            enableInstantInteraction()
        end
    elseif input.KeyCode == Enum.KeyCode.F3 then
        toggleAutoInteract()
    elseif input.KeyCode == Enum.KeyCode.F4 then
        if panel.Visible then
            panel.Visible = false
            floatButton.Visible = true
        else
            panel.Visible = true
            floatButton.Visible = false
        end
    end
end)

-- 初始提示
print("🎮 PLAYBREAK工具箱 v2.0 已加载")
print("📋 快捷键:")
print("  F1 - 开始执行（清理脚本）")
print("  F2 - 启用秒互动")
print("  F3 - 切换自动互动")
print("  F4 - 显示/隐藏UI")